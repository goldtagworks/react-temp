import { useEffect } from 'react';

const MainPage = () => {
    useEffect(() => {
        //

        return () => {
            //
        };
    }, []);

    return <div> d </div>;
};

export default MainPage;
